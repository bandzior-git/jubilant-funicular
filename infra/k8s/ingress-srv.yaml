apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-srv
  annotations:
    nginx.ingress.kubernetes.io/use-regex: 'true'
    cert-manager.io/cluster-issuer: 'lets-encrypt-test'
    nginx.ingress.kubernetes.io/ssl-redirect: 'true'
  labels:
    name: ingress
spec:
  tls:
    - hosts:
        - site.balcerski.eu
      secretName: site-balcerski-eu
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: nginx-srv
                port:
                  number: 80
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-ingress-controller
  namespace: ingress-nginx
data:
  ssl-redirect: 'false'
